# Build app
## Implementation

I'm building a website using Next.js (App Router) + Tailwind CSS, deployed to Vercel.

This is a fresh setup — review scaffold the full project structure and global setup from scratch. Use Context7.

Fetch this design file, read its readme, and implement the relevant aspects of the design. https://api.anthropic.com/v1/design/h/ndd8N_2T078HwY-P5FKeMw?open_file=index.html
Implement: index.html

## Requirements

- Match the mockup layout and design pixel perfect
- Use only Tailwind utility classes — no inline styles, no CSS modules
- All components should be fully responsive (mobile, tablet, desktop)
- Mobile responsive nav with hamburger menu for small screens 
- Use Next.js for all internal navigation 
- All components should be client or server components as appropriate ("use client" only where interactivity is needed)
- If you identify any reusable UI elements (e.g. a card, a section header), extract them into src/components/ui/
- Form fields are UI only — no submission logic needed
- All CTA buttons labelled "Get My Visibility Report" should link to form that has a submit button

# Post process todos
[] Connect google analytics
[] email verification

# Prompts
